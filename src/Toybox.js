'use strict';

export default class Toybox {

    constructor( config ) {
        this.config = config;
        this.__loadServices();
    }

    __loadServices() {

    }

    static get(serviceName = '') {

    }
}